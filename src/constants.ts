/**
 * A custom symbol to mark if ts-node is already used
 */
export const HYPERSE_TS_NODE = Symbol.for('@hyperse/ts-node');
/**
 * A symbol used to mark in "process" object that this library is used
 * in runtime.
 */
export const HYPERSE_TS_NODE_PATHS = Symbol.for('@hyperse/ts-node-paths');
